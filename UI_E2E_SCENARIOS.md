# UI E2E 최소 시나리오 (운영 체크리스트)

실무에서 자주 쓰는 핵심 흐름만 빠르게 점검하기 위한 최소 시나리오입니다.

## 공통 사전 조건
- 앱 실행: `streamlit run app.py`
- `데이터 관리`에서 더미 시나리오 또는 MMP 원본 데이터를 먼저 불러오기

---

## 시나리오 1) 데이터 관리 흐름 점검
1. `데이터 관리` 탭 진입
2. `더미 시나리오 선택` 기본값 유지 후 더미 로드
3. 설치/이벤트/비용 미리보기 표가 나타나는지 확인
4. 아래 항목 다운로드 버튼 클릭 가능 여부 확인
   - 설치 원본 CSV
   - 이벤트 원본 CSV
   - 비용 원본 CSV
5. 템플릿 다운로드 버튼 클릭 가능 여부 확인
   - 설치 템플릿 CSV
   - 이벤트 템플릿 CSV
   - 비용 템플릿 CSV

성공 기준
- 더미 로드 성공 메시지가 노출됨
- 표 3개(설치/이벤트/비용) 렌더링
- 다운로드 버튼 클릭 시 파일 응답이 내려옴

---

## 시나리오 2) 운영 판단 점검
1. `운영 판단` 탭에서 오늘의 UA 운영 현황 확인
2. 증액 테스트/감액 검토/검증 필요/유지 카드와 우선 조정 대상 확인
3. 아래 `세그먼트별 UA 판단` 영역으로 이동
4. 판단 기준 레벨을 `캠페인` 또는 `소재`로 변경
5. 판단 결과 표에 아래 컬럼이 보이는지 확인
   - 판단
   - 세그먼트
   - D7 ROAS
   - 목표 대비
   - 데이터 신뢰도
   - 다음 행동
6. 선택 세그먼트 상세와 성장·품질·데이터 신뢰도 상세를 확인

성공 기준
- 레벨 변경 시 오류 없이 표와 요약 카드가 갱신됨

---

## 시나리오 3) 분석 점검
1. `분석` 탭의 코호트 성장 분석에서 D7 LTV 순위와 운영 해석 확인
2. `라이브옵스 전후 비교` 영역으로 이동
3. 비교 기준 레벨을 `캠페인` 또는 `소재`로 변경
4. 동일 요일 기준 과거 주 수를 변경하고, 가중 D7 LTV 변화·층화 상세 테이블·운영 해석 렌더링 확인
5. 데이터가 거의 없는 기간으로 변경해 빈 결과 안내문 확인

성공 기준
- 정상 기간: 카드/층화 비교 테이블/인과 해석 한계 안내 표시
- 빈 기간: 에러 크래시 없이 안내문 표시

---

## 권장 운영 방식
- 배포 전 최소 1회 위 3개 시나리오 점검
- 회귀 이슈가 생기면 재현 시나리오 번호를 이 문서 기준으로 기록
